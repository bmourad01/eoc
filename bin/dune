(executables
 (names parse_r
        opt_r
        interp_r
        interp_c
        uniquify
        resolve_complex
        explicate_control
        select_instructions
        uncover_live
        color_graph
        allocate_registers)
 (flags -w -33)
 (modes byte native)
 (libraries core_kernel eoc))
