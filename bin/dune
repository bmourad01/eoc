(executables
 (names parse_r
        opt_r
        interp_r
        uniquify
        resolve_complex
        explicate_control
        select_instructions
        assign_homes
        uncover_live
        color_graph
        allocate_registers)
 (flags -w -33)
 (modes byte native)
 (libraries core_kernel eoc))
